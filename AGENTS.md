# 短剧制作 Agent

本文件适用于 `D:/ai/series` 及其下的短剧项目，作为 Codex 的工作入口与执行约定，不是独立后台服务。使用中文沟通。用户本次明确要求优先于本文件的默认值；已确认的需求不重复询问。

## 技能入口

- 制作、继续或修改短剧时，读取 [short-drama-agent](C:/Users/MTSD_/.codex/skills/short-drama-agent/SKILL.md)，并按任务读取其 `references/content-review.md`、`references/render-planning.md`、`references/comfyui.md`。
- 编写或修改视频 Prompt 时，直接读取 [h3-prompt-writing](C:/Users/MTSD_/.codex/skills/h3-prompt-writing/SKILL.md) 和对应模式的官方格式参考：Ref2VA 使用 `references/ref-en.txt`；其他模式按该技能选择。不得只凭记忆或模仿旧 Prompt 声称使用了官方格式。
- 图片生成按可用的 imagegen 技能和工具执行。先查看实际参考，再生成、保存和检查图片。
- 不使用 Director Console 或 `h3-15s-plusplus` 作为默认制作入口，不引入旧故事模板。技能是详细规范，本文件保留工作区约定，避免另建一套相互冲突的流程。

## 启动与恢复

1. 新故事先补齐尚未明确的题材、时长、情绪基调、人物关系及偏好，再给出大纲。不能默认继承旧项目的反转、人物、剧情或结局。
2. 选定大纲且用户要求开始制作后，持续完成已授权步骤。制作前明确目标清晰度、横竖画幅和精确输出尺寸；示例不等于已选参数。
3. 继续项目先读取项目 `project.json`，再读取其引用的大纲、剧本、素材、Prompt、工作流参数和执行记录。以清单指向的有效版本为准，不以文件名排序或旧任务成功状态代替判断。
4. 提交任务前核对实际队列及已记录任务 ID，避免重复提交；状态不明时先查证，不盲目重试。
5. 讨论方案、更新 Agent/技能、检查进度或整理文件本身不启动渲染。用户暂停后，等待明确恢复指令；不能将文件维护请求视为试听通过。

## 固定默认值与可变参数

- 人物固定为**半写实国风／偏写实国漫角色风格**，以 short-drama-agent 内的画风参考为标准。不再提供常规风格选择。固定的是人物表现方式，不是参考人物的身份、服装或背景。
- 角色库为纯白背景三视图：左正面、中背面、右 90 度侧面，等比例完整全身。同一人物全剧复用，人物 ID、服装与声音设定保持一致。
- 默认 **SelfLift 渐进采样＋H3 latent 放大**。只读检测本地显卡、显存、系统内存及节点/模型能力，再配置分辨率、步数、分块与显存策略。需要时查本地源码及官方资料，不未经核对宣称“最优”。
- 通用脚本不写死 RTX 3060、720p、两个人物、五段或 50 秒；项目参数放配置文件。需要放弃默认路线、降低交付清晰度或增加未授权依赖/费用时，说明具体原因与方案。
- 默认每段 **10 秒**，按顺序逐段生成；总时长不能整除 10 秒时明确取舍，不擅自改变时长。
- 默认包含中文对白、环境音、同步音效和无歌词配乐，不加字幕。声音设计服从剧情，对白应清楚可辨。

## 制作流程

1. 完成全剧分场剧本、人物设定、声音计划及每段开头/结尾状态，先保证故事与用户要求一致。
2. 准备并检查角色白底三视图，逐场准备实际符合地点、天气、时间和光线的无人场景参考。已有有效素材直接复用。
3. 将用户要求转为可观察的画面/声音验收条件，记录来源及适用片段。首次渲染前展示简短中文镜头清单，分别写清画面与声音；已确认方向不再次索取例行批准。
4. 每段独立保存完整 H3 Prompt，并由项目及分段清单引用。正式字段、参考标签、对白和声音写法依官方模式规则；不能拿中文概要或对白列表冒充完整 Prompt。
5. 提交前交叉核对**剧本、实际参考图、完整 Prompt、实际绑定 API**，分别记录格式与内容检查。图像槽位、角色 ID、场景、声音输出和采样/放大连接必须与清单一致。
6. 首段通过必要检查后，再继续依赖它的片段。连续段使用前段**已验收、仍有效的真实末帧**及节点支持的引导条件，实际接入工作流。按真实结尾调整动作起点，避免重复伸手、重复台词、站位跳变或道具复位。
7. 每段保存视频、真实尾帧、任务 ID、执行参数和检查证据。完成技术、画面、声音及衔接检查，再推进依赖链。
8. 全部分段验收通过后，自动按清单顺序合并为一个完整视频，检查最终顺序、总时长、尺寸、帧率、可播放性及完整音轨，然后交付。

## 内容检查与失败处理

- 画面要求必须在实际画面体现，不能用台词、旁白或标题替代。例如用户要雪景，必须检查实际积雪/飘雪，不能只让人物说“雪”。此例不是新项目的默认天气。
- 技术成功、画面通过、声音通过、全片完成是不同状态。“有音轨”不代表台词正确；抽帧检查不代表检查了声音。
- 没有可用查看或试听能力时，如实记录未验证，不伪造听取或识别结果，不将其标为通过，不推进依赖链。确需用户试听时提供具体片段与简短核对项；用户确认只适用于对应片段，记录确认来源。
- 上游需求、剧本或素材改变时，按依赖标记受影响的 Prompt、输入检查、执行快照、输出、尾帧和最终合成为 `stale`。历史渲染成功不能自动恢复有效性。
- 每个内容修订下，每段默认首次生成加最多两次针对性重试；重试前定位问题。保留失败证据，不无限重试，也不跳过检查冒充完成。
- 尊重用户人工修改的 UI/API。执行前核对配对与文件指纹，可靠同步后再运行；不得用旧 API 忽略 UI 修改，或通过重建脚本悄悄覆盖人工改动。无法判断冲突时再询问。

## 项目目录与工作流

```text
<项目名>/
  project.json
  00_故事大纲/
  01_完整剧本/
  02_角色库/
  03_分场制作/<场次>/
    <片段>_H3完整提示词_vN.txt
    <片段>/revision_NN/attempt_NN/  # 视频、尾帧、检查证据
  04_全片输出/
  iworkflow/
    main.ui.json                    # 用户可打开、修改的主工作流
    使用说明.md
    main/                           # 与主工作流同名的辅助目录
      main.api.json
      main.pair.json
      profile.json
      segments.json
      <必要共用脚本与环境记录>
      patches/                      # 如有运行时修复，保存差异与验证证据
      history/                      # 必要的主模板历史版本
      runs/<场次>/<片段>/revision_NN/attempt_NN/
```

- 每项目默认只维护一份通用主 UI 工作流。人物、场景、Prompt、种子、时长及连续性关系通过参数绑定；换场景或片段不复制一整套 UI 和共用脚本。
- 只有节点结构确实无法覆盖新的生成模式时才增加必要模板，并配同名辅助目录。逐次执行 API 是快照，不是新增主模板。
- 辅助文件集中保存，根目录不散放调试文件、临时脚本或重复模板。历史记录标明用途，不自动删除用户文件；用户授权清理时先核对引用，避免删除有效素材与恢复证据。
- `project.json` 是恢复入口。每次状态变化原子写入内容修订、阶段、有效文件版本、输入/输出检查、任务 ID、尝试次数、实际输出和阻塞原因。
- 全片真正合并、验收且可播放后，才设置完成状态和 `project.json.final_output`。分段渲染成功或存在候选合成文件都不等于已交付。

## 当前项目入口

当前项目：[借你一场雪/project.json](D:/ai/series/借你一场雪/project.json)。已确认仙侠爱情、甜蜜治愈，女主 18 岁、男主约 30 岁；总长 50 秒，5 段各 10 秒，1280×720 横屏。画面从开场即呈现雪景。

上述故事、人物和尺寸仅属于该项目，不继承为新故事默认值。实时进度、待试听片段、当前 Prompt 和有效工作流版本均读取项目清单，不在本文件维护第二份进度。汇报时明确区分已生成、已验收、待处理及最终成片，不将局部完成表述为全部完成。
