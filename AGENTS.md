# 短剧制作工作区

本文件是Codex在本仓库的入口与执行约定。使用中文；用户本次明确要求优先，已确认的需求不重复询问。

## 规则分工

- 制作、继续或修改短剧：读取[short-drama-agent](.agents/skills/short-drama-agent/SKILL.md)，按任务加载其references。制作流程、画风、角色/场景素材、情绪、人数、动作、音乐和验收规则以该skill为维护源，不在本文件复制。
- 编写H3 Prompt：读取[h3-prompt-writing](.agents/skills/h3-prompt-writing/SKILL.md)及对应官方模式参考；不能凭旧Prompt或记忆冒称官方格式。不使用Director或旧故事模板作为默认入口。
- 图片生成遵循当前可用的imagegen技能和工具；模型/显卡/节点能力以当前电脑实测为准。
- 项目project.json保存本剧需求、有效素材版本、参数引用、进度和审查证据；iworkflow及脚本负责真实执行和可验证的拦截。文档不是独立后台程序。

## 启动与恢复

1. 先判定用户在讨论、更新规则、开始、继续还是暂停；讨论本身不新启动渲染，既有获授权的制作按用户是否暂停继续。
2. 新故事按skill补齐尚未明确的需求并确认大纲；已有项目读取project.json及其引用的有效文件，不按文件名排序猜进度。
3. 检查实际队列及已记录任务ID再提交；状态不明先核实，不盲目重试。只执行经过当前规则审查的输入。
4. 更新通用策略改skill；本剧人物/剧情/尺寸/场次等只改项目清单及依赖文件。受影响输入与输出按skill标记失效，历史运行成功不等于内容通过。
5. 技术通过、画面通过、声音验证与成片完成分别汇报。缺少检查能力如实记录，遵循skill中的已授权豁免范围；已知错误不得豁免。

## 工作区约定

- 项目独立目录；角色和场景按稳定ID集中归档，多视角复用，具体目录见skill。每个项目维护一份可复用主UI工作流和同名辅助目录，执行API为逐次快照。
- 尊重人工编辑：执行前核对UI/API配对及文件指纹，不能以重建脚本覆盖用户修改。无法确定冲突时再说明具体问题。
- 文件移动/清理先核对有效引用和哈希，保留必要来源与执行证据。发布、推送等按用户授权范围执行，不自动将本地更新当作已同步远端。
- 项目首段、首次武戏的人工验收及后续自动检查均按skill执行，不额外增加审批，不因任务恢复重复确认。

视频查看入口：[视频登记.md](视频登记.md)。该表由skill脚本从清单、执行记录和实际文件刷新；反馈及返修规则见skill。

## 当前项目

[借你一场雪/project.json](借你一场雪/project.json)是当前恢复入口。所有故事设定、进度、生成记录和最终输出以该清单为准；本文件不保存第二份进度或将本剧设置变成新项目默认。

## 在其他电脑使用

复制或克隆整个仓库，并在Codex中打开仓库根目录。两项skill位于`.agents/skills/`，可显式调用`$short-drama-agent`，入口也会按相对路径读取它们。技能、官方Prompt格式参考和人物画风资源随仓库携带。

新电脑先检测本地Python、ComfyUI/节点、模型和GPU，再为目标清晰度核对工作流。技能本身不携带大模型权重，也不承诺旧电脑执行参数直接可用。继续旧项目时保留历史证据，更新当前电脑实际执行配置与失效检查。
